[workspace]
members = [
    "platuned/server",
    "platuned/client/rust",
    "libplatune/player",
    "libplatune/management",
    "uniffi-bindgen",
]
resolver = "2"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
