[workspace]
members = ["platuned/server", "platuned/client/rust", "libplatune/player", "libplatune/management"]
resolver = "2"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
